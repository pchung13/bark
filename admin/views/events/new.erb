<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:events, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:events, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:event) %></h2>
    <div class="inner">
      <% form_for :event, url(:events, :create), :class => :form do |f| %>
        <%= partial "events/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
